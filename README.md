# Homework 2: The Playlist Shuffler

Full assignment: `HW2_The_Playlist_Shuffler.md`.

## Run
```bash
python test_playlist.py
```
Complete `playlist.py`. The script checks basic circular-list
correctness, replays the exact Part A Question 3 scenario (removed
`C, F, D, B, E`, survivor `A`), then stress-tests `elimination_shuffle`
across random playlist sizes and `k` values. Success Token prints once
every check passes.

## Submit
1. `HW2_Theory.pdf` (or `.md`)
2. `playlist.py`
3. The Success Token
